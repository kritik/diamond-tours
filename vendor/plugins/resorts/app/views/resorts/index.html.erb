<% content_for :body_content_left do %>
  <ul id="resorts">
    <% @resorts.each do |resort| %>
      <li>
        <%= link_to resort.title, resort_url(resort) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
